#mysql #数据库

> [!文档]



# 概念

## 索引
索引可以加快数据检索速度.因为索引允许数据库快速定位到数据的存储位置，而不需要进行全表扫描。


### PRIMARY KEY
主键索引, 不能重复, 不能为空.
一张表只能有一个主键.
**auto_increment** 关键字只能修饰主键, 但主键不一定是自增的.

### KEY
"KEY"关键字是一个非常重要的概念，它主要用于创建索引和定义外键。



# SQL 命令和解释

查看表结构
```sql
show create table volumes;
```




